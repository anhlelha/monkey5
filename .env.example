# Copy to .env.local and fill in.

AUTH_SECRET=replace-me-with-32-bytes-base64

# REQUIRED in production: set to `true` when behind a reverse proxy or custom
# domain. Prevents Auth.js `UntrustedHost` errors. Safe to leave in dev too.
AUTH_TRUST_HOST=true

# Optional: explicit NextAuth callback base URL. The deploy script
# (scripts/deploy.sh) automatically rewrites this to `http://<VM_IP>:3000`
# on the VM during deploy. Only set here if you need a different base.
# NEXTAUTH_URL=http://localhost:3000


# Google OAuth (optional — Demo credentials provider works without these).
# Create at https://console.cloud.google.com/apis/credentials
#   Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Comma-separated emails that get the admin role on first sign-in.
ADMIN_EMAILS=anhlh48@fpt.com

# SQLite DB lives at prisma/dev.db by default; overrideable here.
# DATABASE_URL="file:./dev.db"

# ─── AI essay grading (admin tab "Setup AI LLMs") ────────────────────────────
# The API key for the selected provider can be stored in the DB via the admin
# UI, OR supplied here as a fallback. If both exist, the DB value wins. Only the
# key for the *selected* provider is used at grading time.
#
# Anthropic (Claude) — get at https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=
#
# Google Gemini — get at https://aistudio.google.com/app/apikey. Also used by
# scripts/draw-figure-gemini.sh (slash command /exam-import → Workflow D).
# GEMINI_API_KEY=
#
# OpenAI (GPT) — get at https://platform.openai.com/api-keys. Also used by
# scripts/draw-figure-codex.sh for figure generation.
# OPENAI_API_KEY=
